order += "cpu_usage"
order += "volume master"
order += "battery 0"
order += "tztime local"

cpu_usage {
	format = "💻%usage"
}

tztime local {
	format = "%d/%m/%Y %H:%M"
}

volume master {
	format = " ♪%volume"
}

battery 0 {
    format = "🔋%percentage"
    path = "/sys/class/power_supply/BAT1/uevent"
    low_threshold = "20"
}
